<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="NoteTaker.Client.Views.NotebooksPage"
             Title="Notebooks">
    <StackLayout VerticalOptions="FillAndExpand">
        <Button Text="All notes" Clicked="btnAllNotes_OnClick" />
        <ListView x:Name="lsvNotebooks">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="10">
                            <Label Text="{Binding Name}" d:Text="{Binding .}" FontSize="20" />
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Button Text="New notebook" Clicked="btnNewNotebook_OnClick" />
    </StackLayout>
</ContentPage>